for(int i=0;i<n;i++){
  int temp = a[i];
  int j = i;
  while(j>0 && temp<a[j]){
    a[j] = a[j-1];
    j--;
  }
  a[j] = temp;
}
//Algorithm Analysis
// Worst, Average case Time Complexity - O(n-square)
// Best Case time complexity - O(n) 
// Space complexity- O(1)
